{
  "name": "ohana-indexer",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone indexer for Ohana Handshake vouch events",
  "type": "module",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.34.0"
  }
}
